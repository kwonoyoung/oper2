const search=document.querySelector('#search'),cards=[...document.querySelectorAll('.works button')],areas=[...document.querySelectorAll('.area')],grid=document.querySelector('#grid'),empty=document.querySelector('#empty'),title=document.querySelector('#resultTitle'),detail=document.querySelector('#detail'),guide=document.querySelector('#guide');function filter(){const q=search.value.trim();title.textContent=q?`“${q}” 검색 결과`:'어떤 업무를 확인하시나요?';cards.forEach(c=>c.classList.toggle('hide',q&&!c.textContent.includes(q)));areas.forEach(a=>a.classList.toggle('hide',![...a.querySelectorAll('.works button')].some(c=>!c.classList.contains('hide'))));empty.hidden=areas.some(a=>!a.classList.contains('hide'))}search.addEventListener('input',filter);document.querySelector('#clear').onclick=()=>{search.value='';filter();search.focus()};cards.forEach(c=>c.onclick=()=>{document.querySelector('#detailName').textContent=c.dataset.name;document.querySelector('#detailSummary').textContent=c.dataset.summary;document.querySelector('#flow').innerHTML=c.dataset.steps.split('|').map((s,i)=>`<div class="step"><span>${String(i+1).padStart(2,'0')}</span><b>${s}</b></div>`).join('');detail.showModal()});detail.querySelectorAll('.close,.primary').forEach(b=>b.onclick=()=>detail.close());document.querySelectorAll('#guideBtn,#noticeGuide').forEach(b=>b.onclick=()=>guide.showModal());guide.querySelectorAll('.close,.primary').forEach(b=>b.onclick=()=>guide.close());[detail,guide].forEach(d=>d.addEventListener('click',e=>{if(e.target===d)d.close()}));
